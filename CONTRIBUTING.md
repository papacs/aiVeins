# 贡献指南

感谢你帮助 AI 概念变得更清楚。这里欢迎新词条、事实更正、边界补充、来源更新、可读性改进和无障碍修复。

## 开始前

1. 搜索现有词条和 Issue，避免重复。
2. 说明真实问题：读者会在什么场景遇到它，容易和什么混淆？
3. 优先查论文、规范、官方文档和有可复现证据的一手实践。
4. 如果定义仍不稳定，使用 `researching`，不要为了“完整”伪装成共识。

## 新增词条

复制 `docs/TERM_TEMPLATE.md` 到 `content/terms/<slug>.md`。文件名必须与 `slug` 一致。

一份合格词条至少需要：

- 一句话人话解释和一个不误导的类比；
- 它解决的问题与明确边界；
- 适用、不适用条件；
- 至少三个常见失败方式；
- 能从现有词条解析到的 `related` 与 `prerequisites`；
- 至少一个 HTTPS 一手来源；
- `YYYY-MM-DD` 格式的最后核验日期；
- 说明机制或判断方法的正文。

运行：

```bash
npm run content:validate
npm run test
npm run lint
```

## 写作风格

- 面向第一次遇到这个概念的人，不假设读者已经懂另一个缩写。
- 用具体条件代替“更先进”“更智能”“最佳实践”等空泛判断。
- 类比帮助建立直觉，但必须在“它不是什么”里修正类比的边界。
- 区分事实、工程经验和作者判断。
- 不复制大段第三方文字；用自己的话总结并链接原始来源。

## Pull Request

保持一次 PR 只解决一个清晰问题。说明：改了什么、依据是什么、哪些地方仍不确定。维护者可能要求将 `verified` 降为 `researching`，或补充反例和失败条件。

提交贡献即表示你同意按项目 MIT 许可证发布你的贡献。
