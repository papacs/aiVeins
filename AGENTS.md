# AGENTS.md

本文件定义 AI 脉络仓库的协作约定，适用于仓库根目录及全部子目录。人类维护者的明确要求优先于本文；如子目录未来增加更具体的 `AGENTS.md`，则以离目标文件最近的说明为准。

## 项目目标

AI 脉络是一份面向中文 AI 学习者和应用开发者的工程决策手册。它重视概念边界、使用条件、失败方式和可核验来源，不追求堆砌术语、追逐短期热点或替代官方文档。

修改时请守住以下原则：

- 先讲清用户要解决的问题，再解释技术名词。
- 同时写明“适合做什么”和“不适合做什么”。
- 区分事实、经验判断和仍有争议的观点。
- 正式词条必须能追溯到可靠来源，并记录最后核验日期。
- 不为增加数量而创建内容薄、边界模糊或互相重复的词条。

## 仓库地图

| 路径                           | 职责                                          |
| ------------------------------ | --------------------------------------------- |
| `content/terms/`               | 词条的唯一内容源；每个概念一份 Markdown       |
| `lib/content.ts`               | 内容 Schema、加载、排序、关系解析与导出       |
| `app/`                         | 页面、路由、SEO 元数据和机器可读接口          |
| `components/`                  | 站点组件；`components/ui/` 主要是基础 UI 原语 |
| `scripts/validate-content.mjs` | 独立内容校验器，供本地与 CI 使用              |
| `tests/`                       | 内容契约与关键行为测试                        |
| `docs/TERM_TEMPLATE.md`        | 新词条模板                                    |
| `.github/`                     | CI、Issue/PR 模板与依赖更新配置               |

内容流向是：`content/terms/*.md` → `lib/content.ts` → 页面、`/glossary.json` 与 `/llms.txt`。不要在页面中复制一份词条数据。

## 开发环境

- Node.js `>=22.13.0`
- npm（以 `package-lock.json` 为准）

```bash
npm install
npm run dev
```

不要提交 `node_modules/`、`dist/`、本地环境变量、凭据或临时产物。除非任务确实需要，不要新增依赖；新增时说明理由，并同步提交锁文件。

## 工作方式

1. 先阅读相关页面、词条、测试和现有约定，再修改。
2. 保持变更聚焦，不顺手重构无关代码或格式化整个仓库。
3. 优先复用现有内容模型、组件和设计令牌。
4. React 页面默认使用服务端组件；只有浏览器交互确有需要时才增加客户端边界。
5. 保持键盘可操作、语义化标签、清晰焦点状态和足够对比度。
6. 修改公共行为、内容字段或数据出口时，同步更新测试和文档。
7. 完成前检查差异，确认没有密钥、构建产物或无关文件。

不要手工编辑构建输出。`components/ui/` 中的基础组件应尽量保持通用，业务逻辑放在站点组件或页面中。

## 内容规范

新增词条从 `docs/TERM_TEMPLATE.md` 复制，并保持文件名与 `slug` 一致。Frontmatter 字段由 `lib/content.ts` 和校验脚本共同约束，不要绕过 Schema。

状态含义：

- `researching`：仍在收集资料或定义存在争议，不能当作确定结论。
- `verified`：结构完整，关键事实、边界与来源已核验。
- `outdated`：内容已确认过期，需优先修订。

写作要求：

- 面向中文读者使用短句和具体例子，首次出现的缩写要展开。
- 明确 `use_when`、`avoid_when`、`boundaries` 和 `pitfalls`，避免只有定义没有决策价值。
- 关系字段只能引用仓库中存在的词条 slug。
- 来源优先使用官方文档、标准、原始论文或一手技术资料；营销页面不能作为关键结论的唯一依据。
- `last_verified` 必须是实际核验日期，不因普通文字润色而更新。
- 不批量生成未经逐条审查的 AI 内容。

## 代码约定

- 使用 TypeScript、ES modules 和项目现有别名。
- 保持数据处理为纯函数；错误信息应能定位到具体文件和字段。
- 不在客户端重复加载服务端已经拥有的数据。
- 样式优先使用 `app/globals.css` 的设计令牌与已有组件组合。
- 面向用户的文字以简体中文为主；代码标识、协议名和产品专名保留原文。
- 公共 API、Schema 或 URL 发生不兼容变化时，必须在 PR 中显式说明。

## 验证要求

按变更范围运行最小必要检查：

| 变更                 | 至少运行                                        |
| -------------------- | ----------------------------------------------- |
| 词条或内容 Schema    | `npm run content:validate`、`npm run test`      |
| 页面、组件或样式     | `npm run lint`、`npm run test`、`npm run build` |
| 配置、依赖或构建链路 | `npm run check`                                 |
| 仅文档               | 检查链接、命令、文件名与实际仓库一致            |

准备提交或声称完成前，应运行：

```bash
npm run check
```

如果因环境限制无法完成某项检查，应准确记录未验证项和原因，不得把未运行描述为通过。

## Git 与评审

- 提交信息使用简洁的 Conventional Commits 风格，例如 `feat: add evaluation term`、`docs: clarify source policy`。
- 一个提交只表达一个清晰意图；不要改写他人的无关改动。
- PR 描述应包含动机、主要变化、验证结果和必要的截图。
- 新功能优先补充最小可验证测试；修复缺陷时尽量先固化回归场景。
- 不使用强制推送覆盖共享分支，除非维护者明确要求。

更多协作细节见 `CONTRIBUTING.md`、`SECURITY.md` 和 `.github/PULL_REQUEST_TEMPLATE.md`。
